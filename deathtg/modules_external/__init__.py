"""External DeathTG user modules package.

Downloaded modules are loaded under this package so relative imports like
`from ..command import command` can resolve against the main `deathtg` package.
"""

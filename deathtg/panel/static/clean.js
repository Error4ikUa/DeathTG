const toggle=document.getElementById('menuToggle');const drawer=document.getElementById('drawer');const overlay=document.getElementById('drawerBackdrop');function openMenu(v){if(!drawer||!overlay)return;drawer.classList.toggle('open',v);overlay.classList.toggle('open',v)}if(toggle)toggle.onclick=()=>openMenu(!drawer.classList.contains('open'));if(overlay)overlay.onclick=()=>openMenu(false);document.querySelectorAll('.mini-menu a').forEach(a=>a.onclick=()=>openMenu(false));

const search=document.getElementById('moduleSearch');if(search){const cards=[...document.querySelectorAll('.module-card')];search.oninput=()=>{const q=search.value.toLowerCase();cards.forEach(c=>{c.style.display=(c.dataset.name||'').toLowerCase().includes(q)?'':'none'})}}

function openModal(id){const m=document.getElementById(id);if(m)m.classList.add('open')}function closeModal(m){if(m)m.classList.remove('open')}document.querySelectorAll('[data-modal-open]').forEach(btn=>btn.addEventListener('click',()=>openModal(btn.dataset.modalOpen)));document.querySelectorAll('[data-modal-close]').forEach(btn=>btn.addEventListener('click',()=>closeModal(btn.closest('.modal'))));document.querySelectorAll('.modal').forEach(m=>m.addEventListener('click',e=>{if(e.target===m)closeModal(m)}));

function setupAvatarCrop(){const form=document.getElementById('avatarForm');const input=document.getElementById('avatarInput');const img=document.getElementById('cropImage');const frame=document.querySelector('.crop-frame');const zoom=document.getElementById('avatarZoom');const reset=document.getElementById('avatarReset');if(!form||!input||!img||!frame||!zoom)return;let file=null,drag=false,startX=0,startY=0,x=0,y=0,z=1,naturalW=0,naturalH=0;function apply(){img.style.transform=`translate(${x}px,${y}px) scale(${z})`}function center(){x=0;y=0;z=Number(zoom.value||1);apply()}input.addEventListener('change',()=>{file=input.files&&input.files[0];if(!file)return;const url=URL.createObjectURL(file);img.onload=()=>{naturalW=img.naturalWidth;naturalH=img.naturalHeight;center()};img.src=url;frame.classList.add('has-image')});zoom.addEventListener('input',()=>{z=Number(zoom.value||1);apply()});if(reset)reset.onclick=()=>{zoom.value=1;center()};frame.addEventListener('pointerdown',e=>{drag=true;startX=e.clientX-x;startY=e.clientY-y;frame.setPointerCapture(e.pointerId)});frame.addEventListener('pointermove',e=>{if(!drag)return;x=e.clientX-startX;y=e.clientY-startY;apply()});frame.addEventListener('pointerup',()=>drag=false);frame.addEventListener('wheel',e=>{e.preventDefault();z=Math.min(3,Math.max(1,z+(e.deltaY<0?.08:-.08)));zoom.value=z;apply()},{passive:false});form.addEventListener('submit',async e=>{if(!file||!img.src)return;e.preventDefault();const size=512;const canvas=document.createElement('canvas');canvas.width=size;canvas.height=size;const ctx=canvas.getContext('2d');const rect=frame.getBoundingClientRect();const scale=Math.max(rect.width/naturalW,rect.height/naturalH)*z;const drawW=naturalW*scale;const drawH=naturalH*scale;const dx=(size-drawW)/2+(x/rect.width)*size;const dy=(size-drawH)/2+(y/rect.height)*size;ctx.fillStyle='#061108';ctx.fillRect(0,0,size,size);ctx.drawImage(img,dx,dy,drawW,drawH);canvas.toBlob(blob=>{const cropped=new File([blob],'avatar.png',{type:'image/png'});const dt=new DataTransfer();dt.items.add(cropped);input.files=dt.files;form.submit()},'image/png',.95)})}setupAvatarCrop();

function drawLineChart(){const el=document.getElementById('lineChart');if(!el)return;let data=[];const holder=document.getElementById('chartData');if(holder){data=[...holder.querySelectorAll('i')].map(i=>({day:i.dataset.day,count:Number(i.dataset.count||0),modules:(i.dataset.modules||'').split(', ').filter(Boolean)}))}if(!data.length){data=[{day:'empty',count:0,modules:['empty']}]}const w=900,h=300,p=38;const max=Math.max(1,...data.map(x=>x.count));let pts=data.map((d,i)=>{const x=p+(data.length<2?0:i*(w-p*2)/(data.length-1));const y=h-p-(d.count/max)*(h-p*2);return {...d,x,y}});let line=pts.map((p,i)=>(i?'L':'M')+p.x+' '+p.y).join(' ');let area=line+` L ${pts.at(-1)?.x||p} ${h-p} L ${pts[0]?.x||p} ${h-p} Z`;el.innerHTML=`<svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none"><path class="area" d="${area}"/><path class="line" d="${line}"/>${pts.map(p=>`<circle class="dot" cx="${p.x}" cy="${p.y}" r="5"><title>${p.day} · ${p.modules.join(', ')||'empty'}</title></circle>`).join('')}</svg>`}drawLineChart();
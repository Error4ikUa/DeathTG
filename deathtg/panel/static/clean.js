const toggle=document.getElementById('menuToggle');const drawer=document.getElementById('drawer');const overlay=document.getElementById('drawerBackdrop');function openMenu(v){if(!drawer||!overlay)return;drawer.classList.toggle('open',v);overlay.classList.toggle('open',v)}if(toggle)toggle.onclick=()=>openMenu(!drawer.classList.contains('open'));if(overlay)overlay.onclick=()=>openMenu(false);document.querySelectorAll('.mini-menu a').forEach(a=>a.onclick=()=>openMenu(false));const search=document.getElementById('moduleSearch');if(search){const cards=[...document.querySelectorAll('.module-card')];search.oninput=()=>{const q=search.value.toLowerCase();cards.forEach(c=>{c.style.display=(c.dataset.name||'').toLowerCase().includes(q)?'':'none'})}}function drawLineChart(){const el=document.getElementById('lineChart');if(!el)return;const data=JSON.parse(el.dataset.points||'[]');const w=900,h=300,p=34;const max=Math.max(1,...data.map(x=>x.count));let pts=data.map((d,i)=>{const x=p+(data.length<2?0:i*(w-p*2)/(data.length-1));const y=h-p-(d.count/max)*(h-p*2);return {...d,x,y}});let line=pts.map((p,i)=>(i?'L':'M')+p.x+' '+p.y).join(' ');let area=line+` L ${pts.at(-1)?.x||p} ${h-p} L ${pts[0]?.x||p} ${h-p} Z`;el.innerHTML=`<svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none"><defs><linearGradient id="g" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#52ff8b" stop-opacity=".28"/><stop offset="1" stop-color="#52ff8b" stop-opacity="0"/></linearGradient></defs><path class="area" d="${area}"/><path class="line" d="${line}"/>${pts.map(p=>`<circle class="dot" cx="${p.x}" cy="${p.y}" r="5"><title>${p.day} · ${p.modules.join(', ')||'empty'}</title></circle>`).join('')}</svg>`}drawLineChart();